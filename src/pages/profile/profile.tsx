import NavigationDashboard from '../../components/navigationDashboard/navigationDashboard';

const Profile = () => {
  return (
    <NavigationDashboard title={'Profile'}>
      <div> Children</div>
    </NavigationDashboard>
  );
};

export default Profile;
